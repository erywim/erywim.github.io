---
import FishSprite from './FishSprite.astro'

interface Props {
  speaker?: string
  /** 是否在文本末尾显示闪烁光标（首页用） */
  cursor?: boolean
  /** 是否显示说话人红鲤头像 */
  avatar?: boolean
  /** 打字机文本：设置后逐字打出（此时忽略 slot） */
  typewriter?: string
  class?: string
}

const { speaker, cursor = false, avatar = false, typewriter, class: className } = Astro.props
const txt = typewriter ?? undefined
---

<div class={`dialogue${className ? ` ${className}` : ''}`}>
  {
    avatar && (
      <div class='avatar'>
        <FishSprite width={44} height={24} label={speaker || 'erywim'} />
      </div>
    )
  }
  <div class='body'>
    <span class='speaker'>{speaker}</span>
    {
      txt !== undefined ? (
        <div class='txt' data-text={txt}></div>
      ) : (
        <div class='txt'>
          <slot />
          {cursor && <span class='cur'>&nbsp;</span>}
        </div>
      )
    }
  </div>
</div>
