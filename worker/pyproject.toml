[project]
name = "erywim-blog-api"
version = "0.1.0"
description = "A tiny Cloudflare Python Worker backed by D1 for Erywim's blog."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
